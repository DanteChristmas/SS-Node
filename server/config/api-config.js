'use strict';
const path = require('path');

(function (apiConfig) {
  apiConfig.init = function (app) {
    // This is where auth verifcation and signature verifcation should live

    //I should make this a _helper middleware function
    
  };

})(module.exports);
